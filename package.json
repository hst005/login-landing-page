{
  "name": "login-landing-page",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Google Sign-In landing page with Android APK (Capacitor)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "sync": "npm run build && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "build:apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "capacitor": {
    "platforms": [
      "android"
    ]
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "@capgo/capacitor-social-login": "^7.20.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0",
    "vite": "^6.3.5"
  }
}
